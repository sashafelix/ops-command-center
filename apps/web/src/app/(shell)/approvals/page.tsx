import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Approvals · Ops Command Center" };

export default function ApprovalsPage() {
  return (
    <Placeholder
      title="Approvals"
      blurb="Human-in-the-loop queue for risky tool calls — A/D/E/S actions land in Phase 2."
      empty="Approvals queue ships in Phase 2."
    />
  );
}
