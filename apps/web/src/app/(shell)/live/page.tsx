import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Live · Ops Command Center" };

export default function LivePage() {
  return (
    <Placeholder
      title="Live"
      blurb="Active / Watching / Recently Done — KPIs and session columns land in Phase 2."
      empty="Live board ships in Phase 2."
    />
  );
}
